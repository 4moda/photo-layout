# 画面・状態・操作カタログ

PhotoLayout の全画面について、**画面の状態**と**操作のバリエーション**を網羅的に列挙する。
各画面に**画面ID**（`S01`…）、各機能に**機能ID**（`F01`…）を振り、スクリーンショットと
1:1で対応づける。実機を動かさずに、CIが撮る画像と本書だけで「どの画面のどの操作が
どう見えるか」を追えるようにするのが目的。

## スクリーンショットの命名規約と紐づけ

スクショ名（`AppTests/PhotoLayoutUITests/ScreenshotSmokeTests.swift` の `snapshot(...)`）は:

```
<画面ID>[-<機能ID>]｜<状態/操作の説明>
例: S01-F02｜プロジェクトなし（空状態）
    S02｜写真1枚（自然配置）        ← 状態のみ（特定機能に紐づかない）
```

fastlane snapshot の出力ファイルは `<言語>/<端末>-<スナップ名>.png`。したがって

| 軸 | 取得元 |
|---|---|
| 言語 | ディレクトリ名（`ja-JP` など） |
| 端末 | ファイル名の先頭（`iPhone 16-…`） |
| 画面ID / 機能ID | スナップ名の先頭トークン |

> **スナップ名に使えない文字**: スナップ名はそのままPNGファイル名になり、GitHub Actions の
> 成果物アップロードは `: " < > | * ? \r \n` と `/`（パス区切り）を拒否する。比率などは
> 全角「：」、区切りは「・」を使う（例: `1：1`, `元画像・各比率`）。区切り記号 `｜` は全角なのでOK。

`AppTests/PhotoLayoutUITests/tools/build_screenshot_index.py` がこの規約でファイル名を解析し、
**言語・画面ID・端末で絞り込めて説明文で検索できる `index.html`** を生成する
（CIの `screenshots` artifact に同梱）。fastlane 標準の `screenshots.html` も併存する
（言語・端末のみの素朴な一覧）。

スクショ運用ツール一式（`fastlane/`・`tools/`）は撮影対象の UITest ターゲットと同じ
`AppTests/PhotoLayoutUITests/` 配下に置く。CIはこのディレクトリをCWDにして `fastlane snapshot`
を実行する（Snapfileのパスは `../../` でリポジトリ直下の xcodeproj / DerivedData を参照）。

## 画面一覧

| 画面ID | 画面 | 実装 | 役割 |
|---|---|---|---|
| S01 | プロジェクト一覧 | `ProjectListView` | 作成済みレイアウトのサムネイル一覧・新規作成・削除 |
| S02 | スライド編集 | `PageEditorView` | シームレスキャンバスでの写真配置・変形・枠・テンプレート・書き出し起点 |
| S03 | スライド俯瞰 | `PageOverviewView` | 全スライドの横並び一覧・挿入/複製/並べ替え/削除・比率/背景 |
| S04 | 書き出しプレビュー | `PreviewView` | 仕上がり確認とカメラロール保存 |

凡例: **撮** = CIで自動撮影しているもの（スクショ名を併記）。空欄は現状未撮影（将来追加候補）。

---

## S01 プロジェクト一覧 (`ProjectListView`)

### 状態

| 状態ID | 状態 | 撮 |
|---|---|---|
| S01-a | 空状態（プロジェクトなし・`ContentUnavailableView`「レイアウトがありません」） | `S01-F02｜プロジェクトなし（空状態）` |
| S01-b | 一覧（複数プロジェクト・サムネイルグリッド） | `S01-F01｜一覧（複数プロジェクト・サムネイル＋ページ数バッジ）` |
| S01-c | セル: 単一ページ（サムネイルのみ） | （S01-bに含む） |
| S01-d | セル: 複数ページ（右下に「Nページ」バッジ） | （S01-bに含む） |

### 操作・機能

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S01-F01 | 起動・一覧表示 | サムネイルグリッド | ✓ |
| S01-F02 | 空状態表示 | プレースホルダ＋作成導線 | ✓ |
| S01-F03 | ＋メニューを開く | 用紙サイズ選択メニュー | `S01-F03｜用紙サイズ選択メニュー` |
| S01-F04 | 新規作成: 正方形 1:1 | 空スライドで S02 へ | `S01-F04｜新規作成 正方形 1：1（空キャンバス）` |
| S01-F05 | 新規作成: 縦 4:5（Instagram） | 空スライドで S02 へ | `S01-F05｜新規作成 縦 4：5（空キャンバス）` |
| S01-F06 | 新規作成: 縦 3:4（X 1枚） | 空スライドで S02 へ | `S01-F06｜新規作成 縦 3：4（空キャンバス）` |
| S01-F07 | 新規作成: 横 16:9 | 空スライドで S02 へ | `S01-F07｜新規作成 横 16：9（空キャンバス）` |
| S01-F08 | 新規作成: 横長 1.91:1（Instagram） | 空スライドで S02 へ | `S01-F08｜新規作成 横長 1.91：1（空キャンバス）` |
| S01-F09 | セルをタップ | S02 スライド編集へ遷移 | （各デモを開いて撮影） |
| S01-F10 | セルの⋯メニュー → 削除 | 一覧から除去 | `S01-F10｜セルの⋯メニュー（削除）` |

---

## S02 スライド編集 (`PageEditorView`)

全スライドを隙間なく横連結したシームレスキャンバス。フッターのメニューは**選択状態で3種に切替**:
何も選択なし=スライド編集メニュー / 写真選択=写真メニュー / クロップ中=完了のみ。

### 状態

| 状態ID | 状態 | 撮 |
|---|---|---|
| S02-a | 空スライド（写真なし・フッター=スライド編集メニュー） | `S02｜新規スライド（空・スライド編集メニュー）` |
| S02-b | 写真1枚（自然配置＝元アスペクトのまま中央付近） | `S02｜写真1枚（自然配置・元アスペクトのまま中央）` |
| S02-c | 写真選択（青い選択枠＋四隅○ハンドル＋辺ハンドル・フッター=写真メニュー） | `S02-F09｜写真を選択（写真メニュー＋四隅ハンドル）` |
| S02-d | クロップ調整モード（オレンジ破線枠＋ヒント・フッター=完了のみ） | `S02-F10｜クロップ調整モード` |
| S02-e | 空スロット表示（テンプレ適用後・グレー塗り＋破線＋＋） | `S02-F14｜田の字を適用（空スロット4つ・グレー範囲）` |
| S02-f | 複数写真の重なり（カスケードずらし配置） | |
| S02-g | 他スライドへ跨る写真（プロジェクト配置モデル・パノラマ） | `S02｜パノラマ（1枚が3スライドに跨る）` |
| S02-h | スナップガイド表示中（ピンクの破線） | |
| S02-i | undo/redo の活性・非活性 | |
| S02-j | 書き出し中（中央↓がスピナー） | |
| S02-k | 種別: コラージュ（田の字4枚合成） | `S02｜コラージュ（田の字4枚の合成）` |
| S02-l | 種別: 枠付き（黒背景＋白フチ＋マット） | `S02｜枠付き（黒背景＋白フチ＋マット）` |
| S02-m | 種別: X組写真（タイムライン合成・左大＋右2） | `S02｜X組写真（タイムライン合成・左大＋右2）` |

### 操作・機能

上部バー:

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S02-F01 | undo | 直前操作を取消 | |
| S02-F02 | redo | 取消を再適用 | |
| S02-F03 | スライド番号表示（N/総数） | 現在位置の把握 | |

フッター（スライド編集メニュー＝何も選択なし）:

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S02-F04 | テンプレートを開く | 型枠ビジュアル一覧シート（S02-F14） | |
| S02-F05 | 写真追加（⊕） | PhotosPicker → 選択枚数ぶん自然配置 | |
| S02-F06 | ダウンロード（中央↓・主役） | S04 書き出しプレビューへ | （S04で撮） |
| S02-F07 | レイヤーを開く | レイヤー順シート（S02-F15） | |
| S02-F08 | スライド（俯瞰）を開く | S03 スライド俯瞰へ | （S03で撮） |

フッター（写真メニュー＝写真選択中）:

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S02-F09 | 枠比率メニュー | 元画像/1:1/4:5/3:4/16:9（写真は歪まずクロップ窓が変わる） | `S02-F09｜枠比率メニュー（元画像・1：1・4：5・3：4・16：9）` |
| S02-F10 | クロップ開始 | クロップ調整モード（S02-d） | ✓ |
| S02-F11 | 枠（縁）シートを開く | 枠プリセット一覧シート（S02-F16） | |
| S02-F12 | 削除 | 選択中の写真を除去 | |

フッター（クロップ中）:

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S02-F13 | クロップ完了 | 通常選択へ戻る | |

シート:

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S02-F14 | テンプレート選択シート | 型枠をマス目でビジュアル表示・選ぶと現スライドに敷く | `S02-F14｜テンプレート選択シート（型枠ビジュアル一覧）` |
| S02-F15 | レイヤー順シート | 現スライドの写真を前面順に並べ上下で入替 | `S02-F15｜レイヤー順シート（重なり順）` |
| S02-F16 | 枠プリセットシート | なし/白フチ/黒フチ/黒太フチ/角丸 をプレビュー選択 | `S02-F16｜枠プリセット一覧シート` |

ジェスチャ（キャンバス直接操作・撮影対象外だが仕様として明記）:

| 機能ID | ジェスチャ | 結果 |
|---|---|---|
| S02-F17 | 写真をドラッグ | 移動（ページ端・中心にスナップ） |
| S02-F18 | 写真をピンチ | アスペクト固定拡縮 |
| S02-F19 | 角○ハンドルをドラッグ | 対角固定のアスペクト固定拡縮 |
| S02-F20 | 辺ハンドルをドラッグ | 枠アスペクト変更（画像は歪まずクロップ窓が変わる） |
| S02-F21 | 写真をダブルタップ | クロップモード切替 |
| S02-F22 | 空スロットをタップ | PhotosPicker → そのスロットへ充填 |
| S02-F23 | 空き地をドラッグ | ビューポートのパン |
| S02-F24 | 非選択でピンチ | ビューポートズーム（十分引くと S03 俯瞰へ） |
| S02-F25 | スライドの地をタップ | 選択解除（スライド編集コンテキストへ） |

---

## S03 スライド俯瞰 (`PageOverviewView`)

全スライドを横並びで一覧。左上=キャンセル（開始時点へ復帰）、右上=完了（確定保存）。

### 状態

| 状態ID | 状態 | 撮 |
|---|---|---|
| S03-a | 複数スライド（現在スライドをアクセント枠でハイライト・末尾に追加カード） | `S03｜スライド一覧（俯瞰）` / `S03｜スライド一覧（3スライド）` |
| S03-b | スライド追加後（枚数が増える） | `S03-F03｜スライドを追加（2スライドに増える）` |

### 操作・機能

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S03-F01 | キャンセル | 開始時点へ復帰し閉じる | |
| S03-F02 | 完了 | 変更を確定・保存して閉じる | |
| S03-F03 | ＋（末尾に追加）/ 追加カード | 末尾に空スライド | ✓ |
| S03-F04 | 長押し: 左に追加 | 該当位置の左に空スライド | `S03-F04〜F09｜カード長押しメニュー` |
| S03-F05 | 長押し: 右に追加 | 該当位置の右に空スライド | 〃 |
| S03-F06 | 長押し: 複製 | 隣に同一スライドを複製 | 〃 |
| S03-F07 | 長押し: 左へ移動 | 1つ前へ | 〃 |
| S03-F08 | 長押し: 右へ移動 | 1つ後へ | 〃 |
| S03-F09 | 長押し: 削除 | スライドを削除（最後の1枚は不可） | 〃 |
| S03-F10 | ドラッグ並べ替え | ドロップ先の位置へ移動 | （ジェスチャ・撮影対象外） |
| S03-F11 | 比率メニュー | カルーセル全体の比率変更（3:4/4:5/1:1/1.91:1/16:9） | `S03-F11｜比率メニュー` |
| S03-F12 | 背景メニュー | プロジェクト共通の背景色（白/薄グレー/グレー/濃グレー/黒） | `S03-F12｜背景メニュー` |

---

## S04 書き出しプレビュー (`PreviewView`)

ダウンロード（↓）で遷移。各スライドの仕上がりを縦スクロールで確認して保存する。

### 状態

| 状態ID | 状態 | 撮 |
|---|---|---|
| S04-a | 単一/複数スライドの仕上がり一覧＋保存バー | `S04｜書き出しプレビュー画面` |
| S04-b | 現在スライドに2枚以上 → 「各写真を個別に保存」ボタンも表示 | `S04-F03｜書き出しプレビュー（各写真を個別に保存ボタン）` |
| S04-c | 書き出し中（保存ボタンがスピナー） | |
| S04-d | 書き出し結果アラート | |

### 操作・機能

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S04-F01 | 閉じる | S02 へ戻る | |
| S04-F02 | カメラロールに保存（全Nスライド） | 各スライドをPNG書き出し・写真ライブラリへ | |
| S04-F03 | 各写真を個別に保存（現スライドが複数枚のとき） | 写真ごとに個別書き出し | ✓ |
| S04-F04 | 書き出し結果アラート | 成功/失敗メッセージ | |

---

## 撮影スイート（テストと画面の対応）

`ScreenshotSmokeTests.swift` の各メソッドが撮る画面:

| テストメソッド | 起動 | 主に撮る画面 |
|---|---|---|
| `testProjectListEmptyAndCreate` | `--reset-store` | S01(空)→S01-F03→S02(空/テンプレ)→S03 |
| `testCanvasAspectsFromCreate` | `--reset-store` | S01-F04〜F08（用紙サイズ別の空キャンバス） |
| `testProjectListPopulated` | `--reset-store --seed-demo` | S01(複数)、S01-F10(⋯削除) |
| `testDemoPhotoOperations` | `--reset-store --seed-demo` | S02(選択/枠比率/クロップ/枠/レイヤー)、S04 |
| `testLayoutKinds` | `--reset-store --seed-demo` | S02(コラージュ/枠付き/パノラマ/X)、S03(比率/背景/長押し)、S04-F03 |

### カバレッジ凡例と方針

各機能は次のいずれかで扱う:

- **✓ / スクショ名あり** — CIで自動撮影。index.html に載る。
- **ジェスチャ・撮影対象外**（S02-F17〜F25, S03-F10） — キャンバス直接操作。離散的な1枚の画像に馴染まないため、**結果状態**（配置後の見た目など）で間接的に確認する。
- **システムUI・撮影不可**（S02-F05 写真追加, S04-F02 カメラロール保存） — PhotosPicker / 写真ライブラリは別プロセス or 権限ダイアログのため、UIテストからの撮影は行わない。
- **アクション（遷移のみ）**（S03-F01/F02 キャンセル/完了, S04-F01 閉じる） — 固有の画面を持たないため撮らない（遷移先の画面で確認）。

この方針により「撮っていない＝抜け」ではなく、**撮らない理由が上のどれか**を各行で判別できる。

`--reset-store` はインメモリ・ストアで起動し、毎回空から始めることで状態を決定論的にする
（`AppComposition.container`）。デモ投入は `DemoProjectSeeder`。

新しい状態を撮りたいときは、対応するテストメソッドに `snapshot("<画面ID>[-<機能ID>]｜説明")`
を1行足すだけ。命名規約さえ守れば index.html のフィルタに自動で載る。
