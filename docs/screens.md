# 画面・状態・操作カタログ

PhotoLayout の全画面について、**画面の状態**と**操作のバリエーション**を網羅的に列挙する。
各画面に**画面ID**（`S01`…）、各機能に**機能ID**（`F01`…）を振り、スクリーンショットと
1:1で対応づける。実機を動かさずに、CIが撮る画像と本書だけで「どの画面のどの操作が
どう見えるか」を追えるようにするのが目的。

## スクリーンショットの命名規約と紐づけ

スクショ名（`AppTests/PhotoLayoutUITests/ScreenshotSmokeTests.swift` の `snapshot(...)`）は:

```
<画面ID>[-<機能ID>]-action-in-english
例: S01-F02-project-list-empty-state
    S02-single-photo-natural-placement
```

fastlane snapshot の出力ファイルは安全なASCII名の

```
<言語>/<端末slug>--<画面ID[-機能ID]-action-in-english>.png
例: ja-JP/iphone-16--S01-F02-project-list-empty-state.png
```

となる。`build_screenshot_index.py` はこの規約でファイル名を解析して `index.html` を生成する。したがって

| 軸 | 取得元 |
|---|---|
| 言語 | ディレクトリ名（`ja-JP` など） |
| 端末 | ファイル名の `--` より前（`iphone-16` など） |
| 画面ID / 機能ID | スナップ名の先頭トークン |

> **使う文字**: スナップ名はそのままPNGファイル名になるため、`A-Z a-z 0-9 -` のみを使う。
> 端末名も `SnapshotHelper.swift` 側で slug 化して保存する。

`AppTests/PhotoLayoutUITests/tools/build_screenshot_index.py` がこの規約でファイル名を解析し、
**言語・画面ID・端末で絞り込めて説明文で検索できる `index.html`** を生成する
（CIの `screenshots` artifact に同梱）。fastlane 標準の `screenshots.html` も併存する
（言語・端末のみの素朴な一覧）。

スクショ運用ツール一式（`fastlane/`・`tools/`）は撮影対象の UITest ターゲットと同じ
`AppTests/PhotoLayoutUITests/` 配下に置く。CIはこのディレクトリをCWDにして `fastlane snapshot`
を実行する（Snapfileのパスは `../../` でリポジトリ直下の xcodeproj / DerivedData を参照）。

## 画面一覧

| 画面ID | 画面 | 実装 | 役割 |
|---|---|---|---|
| S01 | プロジェクト一覧 | `ProjectListView` | 作成済みレイアウトのサムネイル一覧・新規作成・削除 |
| S02 | スライド編集 | `PageEditorView` | シームレスキャンバスでの写真配置・変形・枠・テンプレート・書き出し起点 |
| S03 | スライド俯瞰 | `PageOverviewView` | 全スライドの横並び一覧・挿入/複製/並べ替え/削除・比率/背景 |
| S04 | 書き出しプレビュー | `PreviewView` | 仕上がり確認とカメラロール保存 |

凡例: **撮** = CIで自動撮影しているもの（スクショ名を併記）。空欄は現状未撮影（将来追加候補）。

---

## S01 プロジェクト一覧 (`ProjectListView`)

### 状態

| 状態ID | 状態 | 撮 |
|---|---|---|
| S01-a | 空状態（プロジェクトなし・`ContentUnavailableView`「レイアウトがありません」） | `S01-F02-project-list-empty-state` |
| S01-b | 一覧（複数プロジェクト・サムネイルグリッド） | `S01-F01-project-list-populated` |
| S01-c | セル: 単一ページ（サムネイルのみ） | （S01-bに含む） |
| S01-d | セル: 複数ページ（右下に「Nページ」バッジ） | （S01-bに含む） |

### 操作・機能

下部フローティングメニュー（S02 `PageEditorView` の `slideControls` と同じ位置・見た目。一覧が空でも1件以上でも常時表示。ナビゲーションバー右上には＋を置かない）:

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S01-F01 | 起動・一覧表示 | サムネイルグリッド | ✓ |
| S01-F02 | 空状態表示 | プレースホルダ＋下部フローティング＋での作成導線 | ✓ |
| S01-F03 | 下部フローティング＋を開く | 用紙サイズ選択メニュー | `S01-F03-project-list-paper-size-menu` |
| S01-F04 | 新規作成: 正方形 1:1 | 空スライドで S02 へ | `S01-F04-create-square-canvas` |
| S01-F05 | 新規作成: 縦 4:5（Instagram） | 空スライドで S02 へ | `S01-F05-create-portrait-4-5-canvas` |
| S01-F06 | 新規作成: 縦 3:4（X 1枚） | 空スライドで S02 へ | `S01-F06-create-portrait-3-4-canvas` |
| S01-F07 | 新規作成: 横 16:9 | 空スライドで S02 へ | `S01-F07-create-landscape-16-9-canvas` |
| S01-F08 | 新規作成: 横長 1.91:1（Instagram） | 空スライドで S02 へ | `S01-F08-create-landscape-1-91-1-canvas` |
| S01-F09 | セルをタップ | S02 スライド編集へ遷移 | （各デモを開いて撮影） |
| S01-F10 | セルの⋯メニュー → 削除 | 確認シートを表示（即削除はしない） | `S01-F10-project-cell-delete-menu` |
| S01-F11 | 削除確認シート（拡大サムネイル＋タイトル＋ページ数を表示） → 「削除」を確定 | 一覧から除去。「キャンセル」なら一覧に残る | `S01-F11-project-cell-delete-confirm` |

---

## S02 スライド編集 (`PageEditorView`)

全スライドを隙間なく横連結したシームレスキャンバス。フッターのメニューは**選択状態で3種に切替**:
何も選択なし=スライド編集メニュー / 写真選択=写真メニュー / クロップ中=完了のみ。

### 状態

| 状態ID | 状態 | 撮 |
|---|---|---|
| S02-a | 空スライド（写真なし・フッター=スライド編集メニュー） | `S02-empty-slide-editor` |
| S02-b | 写真1枚（自然配置＝元アスペクトのまま中央付近） | `S02-single-photo-natural-placement` |
| S02-c | 写真選択（青い選択枠＋四隅○ハンドル＋辺ハンドル・フッター=写真メニュー） | `S02-F09-photo-selected-menu` |
| S02-d | クロップ調整モード（オレンジ破線枠＋ヒント・フッター=完了のみ） | `S02-F10-crop-mode` |
| S02-e | 空スロット表示（テンプレ適用後・グレー塗り＋破線＋＋） | `S02-F14-four-grid-empty-slots` |
| S02-f | 複数写真の重なり（カスケードずらし配置） | |
| S02-g | 他スライドへ跨る写真（プロジェクト配置モデル・パノラマ） | `S02-panorama-three-slides` |
| S02-h | スナップガイド表示中（ピンクの破線） | |
| S02-i | undo/redo の活性・非活性 | `S02-undo-redo-active` |
| S02-j | 書き出し中（右上の保存ボタンがスピナー） | |
| S02-k | 種別: コラージュ（田の字4枚合成） | `S02-collage-four-grid` |
| S02-l | 種別: 枠付き（黒背景＋白フチ＋マット） | `S02-framed-black-background` |
| S02-m | 種別: X組写真（タイムライン合成・左大＋右2） | `S02-x-timeline-composite` |

### 操作・機能

上部バー:

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S02-F01 | undo | 直前操作を取消 | |
| S02-F02 | redo | 取消を再適用 | |
| S02-F03 | スライド番号表示（N/総数） | 現在位置の把握 | |

フッター（スライド編集メニュー＝何も選択なし・フローティング）:

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S02-F04 | テンプレートを開く | 型枠ビジュアル一覧シート（S02-F14） | |
| S02-F05 | 写真追加（中央の⊕） | PhotosPicker → 選択枚数ぶん自然配置 | |
| S02-F06 | 書き出し（右上） | S04 書き出しプレビューへ | （S04で撮） |
| S02-F07 | レイヤーを開く | レイヤー順シート（S02-F15） | |
| S02-F08 | スライド（俯瞰）を開く | S03 スライド俯瞰へ | （S03で撮） |

フッター（写真メニュー＝写真選択中）:

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S02-F09 | 枠比率シート | 元画像/3:4/4:5/1:1/16:9 を形で選ぶ（写真は歪まずクロップ窓が変わる） | `S02-F09-frame-aspect-menu` |
| S02-F10 | クロップ開始 | クロップ調整モード（S02-d） | ✓ |
| S02-F11 | 枠（縁）シートを開く | 枠プリセット一覧シート（S02-F16） | |
| S02-F12 | 削除 | 選択中の写真を除去 | |

フッター（クロップ中）:

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S02-F13 | クロップ完了 | 通常選択へ戻る | |

シート:

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S02-F14 | テンプレート選択シート | 型枠をマス目でビジュアル表示・選ぶと現スライドに敷く | `S02-F14-template-sheet` |
| S02-F15 | レイヤー順シート | 現スライドの写真を前面順に並べ、ドラッグまたは上下ボタン（境界disabled）で入替 | `S02-F15-layer-order-sheet` |
| S02-F16 | 枠プリセットシート | なし/白フチ/黒フチ/黒太フチ/角丸 をプレビュー選択 | `S02-F16-frame-preset-sheet` |

ジェスチャ（キャンバス直接操作・撮影対象外だが仕様として明記）:

| 機能ID | ジェスチャ | 結果 |
|---|---|---|
| S02-F17 | 写真をドラッグ | 移動（ページ端・中心にスナップ） |
| S02-F18 | 写真をピンチ | アスペクト固定拡縮 |
| S02-F19 | 角○ハンドルをドラッグ | 対角固定のアスペクト固定拡縮 |
| S02-F20 | 辺ハンドルをドラッグ | 枠アスペクト変更（画像は歪まずクロップ窓が変わる） |
| S02-F21 | 写真をダブルタップ | クロップモード切替 |
| S02-F22 | 空スロットをタップ | PhotosPicker → そのスロットへ充填 |
| S02-F23 | 空き地をドラッグ | ビューポートのパン |
| S02-F24 | 非選択でピンチ | ビューポートズーム（十分引くと S03 俯瞰へ） |
| S02-F25 | スライドの地をタップ | 選択解除（スライド編集コンテキストへ） |

---

## S03 スライド俯瞰 (`PageOverviewView`)

全スライドを横並びで一覧。左上=キャンセル（開始時点へ復帰）、右上=完了（確定保存）。

### 状態

| 状態ID | 状態 | 撮 |
|---|---|---|
| S03-a | 複数スライド（現在スライドをアクセント枠でハイライト・末尾追加カード＋ページ下側の追加ボタン） | `S03-slide-overview` / `S03-slide-overview-three-pages` |
| S03-c | ページ選択中（複製/削除のフローティングメニュー） | `S03-F06-F09-page-selected-menu` |
| S03-b | スライド追加後（枚数が増える） | `S03-F03-append-slide` |

### 操作・機能

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S03-F01 | キャンセル | 開始時点へ復帰し閉じる | |
| S03-F02 | 完了 | 変更を確定・保存して閉じる | |
| S03-F03 | ＋（末尾に追加）/ 追加カード | 末尾に空スライド | ✓ |
| S03-F04 | ページ下側の追加ボタン | 該当位置の右に空スライド | |
| S03-F06 | ページ選択 → 複製 | 隣に同一スライドを複製 | `S03-F06-F09-page-selected-menu` |
| S03-F09 | ページ選択 → 削除 | スライドを削除（最後の1枚は不可） | `S03-F06-F09-page-selected-menu` |
| S03-F10 | ドラッグ並べ替え | ドロップ先の位置へ移動 | （ジェスチャ・撮影対象外） |
| S03-F11 | 比率メニュー | ページ非選択時にカルーセル全体の比率変更（3:4/4:5/1:1/16:9/1.91:1） | `S03-F11-carousel-ratio-menu` |
| S03-F12 | 背景メニュー | ページ非選択時にプロジェクト共通の背景色（白/薄グレー/グレー/濃グレー/黒） | `S03-F12-project-background-menu` |

---

## S04 書き出しプレビュー (`PreviewView`)

右上の書き出しボタンで遷移。複数スライドは左右スワイプのカルーセル（`TabView(.page)`）で1枚ずつ仕上がりを確認して保存・共有する。

### 状態

| 状態ID | 状態 | 撮 |
|---|---|---|
| S04-a | 単一/複数スライドの仕上がり（1ページ全画面）＋保存・共有バー | `S04-export-preview`（単一）／`S04-export-preview-multi-page`（複数、カルーセル1枚目・ドットインジケーター1個目強調）／`S04-export-preview-multi-page-swiped`（同、左スワイプ後の2枚目。ドットの強調位置が1枚目と異なることを確認用） |
| S04-c | 書き出し中（保存ボタンがスピナー） | |
| S04-d | 書き出し結果アラート | |

### 操作・機能

| 機能ID | 操作 | 結果 | 撮 |
|---|---|---|---|
| S04-F01 | 閉じる | S02 へ戻る | |
| S04-F02 | カメラロールに保存（全Nスライド） | 各スライドをPNG書き出し・写真ライブラリへ | |
| S04-F03 | 左右スワイプ（複数スライド時） | カルーセルのページを切り替え、ドットインジケーターの強調位置がスワイプ先のページに連動する | （S04-aで撮） |
| S04-F04 | 書き出し結果アラート | 成功/失敗メッセージ | |
| S04-F05 | 共有（`UIActivityViewController`） | 単一スライドは1枚、複数スライドは投稿順（orderedPages）で全Nスライドをまとめてシステム共有シートへ渡す。カメラロールへは保存しない | |

---

## 撮影スイート（テストと画面の対応）

`ScreenshotSmokeTests.swift` の各メソッドが撮る画面:

| テストメソッド | 起動 | 主に撮る画面 |
|---|---|---|
| `testProjectListEmptyAndCreate` | `--reset-store` | S01(空)→S01-F03→S02(空/テンプレ)→S03 |
| `testCanvasAspectsFromCreate` | `--reset-store` | S01-F04〜F08（用紙サイズ別の空キャンバス） |
| `testProjectListPopulated` | `--reset-store --seed-demo` | S01(複数)、S01-F10(⋯削除メニュー)、S01-F11(削除確認シート) |
| `testDemoPhotoOperations` | `--reset-store --seed-demo` | S02(選択/枠比率/undo-redo/クロップ/枠/レイヤー)、S04 |
| `testLayoutKinds` | `--reset-store --seed-demo` | S02(コラージュ/枠付き/パノラマ/X)、S03(比率/背景/ページ選択)、S04(複数ページ) |
| `testDarkModeSpotCheck` | `--reset-store --seed-demo`（ダーク固定） | S01/S02/S03/S04 各1枚の最小巡回（`-dark`サフィックス） |

### カバレッジ凡例と方針

各機能は次のいずれかで扱う:

- **✓ / スクショ名あり** — CIで自動撮影。index.html に載る。
- **ジェスチャ・撮影対象外**（S02-F17〜F25, S03-F10） — キャンバス直接操作。離散的な1枚の画像に馴染まないため、**結果状態**（配置後の見た目など）で間接的に確認する。
- **システムUI・撮影不可**（S02-F05 写真追加, S04-F02 カメラロール保存, S04-F05 共有） — PhotosPicker / 写真ライブラリ / 共有シートは別プロセス or 権限ダイアログのため、UIテストからの撮影は行わない。
- **アクション（遷移のみ）**（S03-F01/F02 キャンセル/完了, S04-F01 閉じる） — 固有の画面を持たないため撮らない（遷移先の画面で確認）。
- **ナビゲーション上到達不能**（S02-j 書き出し中スピナー） — `isExporting` は `PreviewView`（`.fullScreenCover` で全画面を覆う）の保存ボタン経由でしかtrueにならず、S02編集画面のツールバー自体は書き出し中は画面に出ない。実際に撮れる同等の状態はS04-c（`PreviewView`保存ボタンのスピナー）だが、こちらも非同期処理完了前の一瞬を捉える必要がありタイミング依存のため未着手。
- **タイミング依存・未着手**（S02-h スナップガイド） — ドラッグ中の一瞬だけ表示され、指を離すと同期的に消える（`endGesture()`）。標準のXCUITest APIでは確実に撮れず、バックグラウンドスレッドからの撮影など実装コストが見合わないため見送り。

この方針により「撮っていない＝抜け」ではなく、**撮らない理由が上のどれか**を各行で判別できる。

`--reset-store` はインメモリ・ストアで起動し、毎回空から始めることで状態を決定論的にする
（`AppComposition.container`）。デモ投入は `DemoProjectSeeder`。

新しい状態を撮りたいときは、対応するテストメソッドに `snapshot("<画面ID>[-<機能ID>]-action-in-english")`
を1行足すだけ。命名規約さえ守れば index.html のフィルタに自動で載る。
