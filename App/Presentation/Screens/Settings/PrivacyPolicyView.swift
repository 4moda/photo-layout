import SwiftUI

/// プライバシーポリシー本文。人間による最終的な法的レビューを別途前提とした草稿（本Issueの対象外）。
/// アカウント機能・通信を持たないオンデバイス処理アプリであることを踏まえた内容。
struct PrivacyPolicyView: View {
    var body: some View {
        ScrollView {
            Text(Self.body)
                .font(.callout)
                .frame(maxWidth: .infinity, alignment: .leading)
                .padding(20)
        }
        .background(PLColor.background)
        .navigationTitle("プライバシーポリシー")
        .navigationBarTitleDisplayMode(.inline)
        .toolbar {
            ToolbarItem(placement: .principal) {
                Text("プライバシーポリシー").plStamp(size: 14)
            }
        }
        .accessibilityIdentifier("privacyPolicy.scroll")
    }

    private static let body = """
    PhotoLayout（以下「本アプリ」）は、利用者のプライバシーを尊重し、以下の方針に基づき本アプリを提供します。

    第1条（データの取り扱い）
    本アプリは、写真のトリミング・枠付け・複数枚のレイアウト配置と、SNS投稿向けの高解像度画像書き出しに関する処理をすべて利用者の端末上（オンデバイス）で行います。外部サーバーとの通信は一切行わず、利用者が選択した写真やレイアウト内容が本アプリの開発者や第三者に送信・収集されることはありません。

    第2条（アカウント情報）
    本アプリはアカウント登録・サインイン機能を持ちません。氏名・メールアドレスその他の個人を識別する情報の入力・取得は行いません。

    第3条（写真ライブラリへのアクセス）
    本アプリは、書き出した画像をカメラロールに保存するための追加専用の権限（`NSPhotoLibraryAddUsageDescription`）のみを利用者に要求します。この権限は画像の書き込み（追加）のみを許可するものであり、既存の写真ライブラリの内容を読み取る権限は要求しません。写真の選択には、システム標準の写真選択UI（PhotosPicker）を使用し、選択された写真そのものへのアクセスも本アプリ内の処理に限られます。

    第4条（外部送信・第三者提供）
    本アプリは広告配信・分析目的を含め、いかなる利用者データも外部サービスへ送信しません。第三者への写真データやその他情報の提供も行いません。

    第5条（Cookie等のトラッキング技術）
    本アプリはオンデバイス処理のみで動作するため、Cookie・広告識別子等のトラッキング技術は使用しません。

    第6条（ポリシーの変更）
    開発者は、必要と判断した場合、本ポリシーを予告なく変更することがあります。変更後のポリシーは、本アプリ内での表示をもって効力を生じるものとします。重要な変更がある場合は、可能な範囲でアプリ内の表示等により周知するよう努めます。

    第7条（連絡先）
    本ポリシーに関するお問い合わせは、本アプリの配布ページに記載の連絡先までお願いいたします。

    制定日: 2026年7月14日
    """
}
